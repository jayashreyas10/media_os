{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "node scripts/prepare-db.js && prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "node scripts/prepare-db.js && prisma generate && prisma db push && node scripts/seed-if-empty.js && npm start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
